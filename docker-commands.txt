# powershell commands
docker run `
  -v /${pwd}:/app/project `
  -v /${pwd}/models/rasa_core:/app/models `
  rasa/rasa_core:latest `
  train `
    --domain project/domain_meeting.yml `
    --stories project/data/stories_meeting.md `
    --out models

docker run `
  -v ${pwd}:/app/project `
  -v ${pwd}/models/rasa_nlu:/app/models `
  rasa/rasa_nlu:latest-spacy `
  run `
    python -m rasa_nlu.train `
    -c config.yml `
    -d project/data/nlu_meeting.md `
    -o models `
    --project current

	
docker run `
  -it `
  -v ${pwd}/models/rasa_core:/app/models `
  rasa/rasa_core:latest `
  start `
    --core models
	

docker run `
  -v ${pwd}:/app/project `
  -v ${pwd}/models/rasa_nlu:/app/models `
  rasa/rasa_nlu:latest-spacy `
  run `
    python -m rasa_nlu.train `
    -c config.yml `
    -d project/data/nlu.md `
    -o models `
    --project current